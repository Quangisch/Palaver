<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    android:id="@+id/coordinatorlayout_login">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:id="@+id/linearlayout_login"
        tools:context=".mvp.view.login.LoginActivity">

        <include android:id="@+id/toolbar_login" layout="@layout/toolbar"/>

        <TextView
            android:id="@+id/textview_loginactivity_header"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="@dimen/small_margin"
            android:layout_marginLeft="50dp"
            android:layout_marginRight="50dp"
            android:textSize="@dimen/fontSize_header"
            android:text="@string/login_welcome"
            android:layout_weight="1"/>

        <LinearLayout
            android:id="@+id/fragment_container"
            android:layout_marginTop="20dp"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:orientation="vertical"
            android:layout_weight="10"/>
    </LinearLayout>
</android.support.design.widget.CoordinatorLayout>

